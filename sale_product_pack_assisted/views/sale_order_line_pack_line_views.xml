<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- sale order line pack line -->
    <record model="ir.ui.view" id="view_sale_line_pack_line_list">
        <field name="name">sale.line.pack.line.list</field>
        <field name="model">sale.order.line.pack.line</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="currency_id" invisible="True" />
                <field name="product_id" groups="base.group_user" />
                <field
                    name="price_unit"
                    widget="monetary"
                    options="{'currency_field': 'currency_id'}"
                />
                <field name="product_uom_qty" />
                <field name="discount" groups="sale.group_discount_per_so_line" />
                <field
                    name="price_subtotal"
                    widget="monetary"
                    options="{'currency_field': 'currency_id'}"
                />
            </list>
        </field>
    </record>
</odoo>
